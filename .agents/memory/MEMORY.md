# Memory Index

- [Delayed-completion closures in React Native](rn-delayed-completion-refs.md) — setTimeout callbacks after a celebration/animation delay must read latest state via refs, not render-time closures, or the "next step" can silently fail to advance.
